{
    "name": "PIA",
    "version": "1.0.0",
    "scripts": {
        "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\"",
        "install-all": "concurrently \"npm install -verbose --prefix backend\" \"npm install -verbose --prefix frontend\""
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}
